# Distributed Configurations
spring:
  cloud:
    consul:
      host: host.docker.internal
      port: 8500
      config:
        format: YAML
        enabled: true
        prefix: config
        data: data